name: nayax_assignment
description: nayax' job interview assignment

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.4
  equatable: ^2.0.0
  retrofit:
  cupertino_icons: ^1.0.2
  rxdart: ^0.27.3
  flutter_bloc: ^8.0.1
  floor: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator:
  floor_generator:
  build_runner: ^2.1.8



flutter:

  uses-material-design: true
